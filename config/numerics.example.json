{
  "schema_version": 2,
  "procedure": "dynamic_implicit",
  "application": "moderate_dissipation",
  "nlgeom": true,
  "initial_acceleration_policy": "bypass",
  "initial_increment_s": 0.001,
  "minimum_increment_s": 1e-8,
  "maximum_increment_s": 0.02,
  "maximum_increments": 10000,
  "automatic_stabilization": false,
  "cpus_per_job": 4,
  "maximum_concurrent_solves": 1,
  "max_attempts_including_base": 2,
  "solver_wall_limit_s": 7200,
  "stagnation_observe_only": true,
  "stagnation_window_successful_increments": 100,
  "stagnation_min_wall_window_s": 300,
  "stagnation_progress_fraction": 1e-4,
  "note": "Prototype settings and initial budgets. First collect pilot measurements; do not interpret them as universal optimal settings. Retry #2 is enabled only after a concrete preset is validated."
}
